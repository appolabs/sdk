---
title: Aggiorna un'app
description: Mantieni i contenuti e i metadati dell'app sincronizzati con il tuo sito.
---

La tua app segue il tuo sito: gli aggiornamenti dei contenuti web sono attivi
non appena il sito viene pubblicato, senza alcuna azione necessaria. Le
modifiche a nome e icona aggiornano subito il record e raggiungono i
dispositivi con la prossima build operata, eseguita da Appo.

<Tabs items={['Dashboard', 'CLI', 'MCP']}>
<Tab value="Dashboard">

Gli aggiornamenti dei contenuti non richiedono alcuna azione: pubblica il sito
e l'app riflette la modifica immediatamente.

Per modifiche a nome o icona, apri le impostazioni dell'app su
[apps.goappo.io](https://apps.goappo.io) e aggiorna i campi. Il record si
aggiorna subito; i dispositivi ricevono la modifica con la prossima build operata.

</Tab>
<Tab value="CLI">

```bash
appo apps update <id> [--name <n>] [--url <u>] [--icon <https-url>]
```

Ometti qualsiasi flag per lasciare invariato quel campo. `--url` da solo è
una modifica solo ai contenuti; `--name` e `--icon` aggiornano il record e
raggiungono i dispositivi con la prossima build operata.

</Tab>
<Tab value="MCP">

Chiedi al tuo assistente di aggiornare i campi dei contenuti: chiamerà
`configure_app` con `name` e/o `base_url`. Per le modifiche all'icona
chiamerà `set_icon`.

</Tab>
</Tabs>

Le modifiche web sono attive non appena il sito viene pubblicato. Nome e icona si aggiornano subito sul record e raggiungono i dispositivi con la prossima build operata, eseguita da Appo.
